use ast::{Expr, Function, FunctionArg, FunctionStatement, Literal, Operator, Program};

pub unsafe fn codegen(input: Vec<Program>) {
    for program in input {}
}

pub fn codegen_program(program: Program) {
    match program {
        Program::Function(function) => {}
    }
}
